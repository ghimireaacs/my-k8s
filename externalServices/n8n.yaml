# 1. Service
apiVersion: v1
kind: Service
metadata:
  name: n8n-service
  namespace: personal
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80          # Traefik talks to 80
      targetPort: 5678  # Maps to n8n container port
---
# 2. EndpointSlice (The Bridge)
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: n8n-slice
  namespace: personal
  labels:
    kubernetes.io/service-name: n8n-service
addressType: IPv4
ports:
  - name: http
    port: 5678
    protocol: TCP
endpoints:
  - addresses:
      - "10.10.10.114"
    conditions:
      ready: true
---
# 3. Ingress (The Route)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n-ingress
  namespace: personal
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod-cf"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "n8n.560384.xyz"
      secretName: n8n-tls-secret
  rules:
    - host: "n8n.560384.xyz"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n8n-service
                port:
                  number: 80
