# ServersTransport: Skip TLS verification for Proxmox self-signed cert
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: proxmox-transport
  namespace: personal
spec:
  insecureSkipVerify: true
---
# Service: Virtual cluster service pointing to external Proxmox
apiVersion: v1
kind: Service
metadata:
  name: proxmox-service
  namespace: personal
  annotations:
    traefik.ingress.kubernetes.io/service.serverstransport: personal-proxmox-transport@kubernetescrd
spec:
  clusterIP: None
  ports:
    - name: https
      port: 8006
      targetPort: 8006
      protocol: TCP
---
# EndpointSlice: Maps service to actual Proxmox IP
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: proxmox-slice
  namespace: personal
  labels:
    kubernetes.io/service-name: proxmox-service
addressType: IPv4
ports:
  - name: https
    port: 8006
    protocol: TCP
endpoints:
  - addresses:
      - "10.10.10.150"
    conditions:
      ready: true
---
# Ingress: Public route with SSL backend
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: proxmox-ingress
  namespace: personal
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod-cf"
    traefik.ingress.kubernetes.io/service.serversscheme: https
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "proxmox.560384.xyz"
      secretName: proxmox-tls-secret
  rules:
    - host: "proxmox.560384.xyz"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: proxmox-service
                port:
                  number: 8006
